---
import { Icons } from './icons.ts'

interface Path {
  d: string
  class?: string
}

const { name, class: externalClass } = Astro.props

const icon = (Icons as any)[name] || {}

const paths: Path[] = icon.paths || [];
const iconClass = externalClass ? `${icon.class || ''} ${externalClass}`.trim() : icon.class;
---

{
  icon
? (
    <svg
        class={iconClass}
        height={icon.height}
        viewBox={icon.viewBox}
        width={icon.width}
        fill={icon.fill}
        clip-rule={icon.clipRule}
        fill-rule={icon.fillRule}
        stroke={icon.stroke}
        stroke-width={icon.strokeWidth}
        stroke-linecap={icon.strokeLinecap}
        stroke-linejoin={icon.strokeLinejoin}
    ><title>{icon.title}</title>
        {paths.map(path => (
            <path d={path.d} class={path.class || ''} />
        ))}
    </svg>
)
: (
      'Icon not found'
  )
}
